<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
    <!-- Add EJS code here -->
  <% include ./partials/_header %>

  <% if (user) { %>
    <h3>ShortURL: <%= shortURL %> </h3>
    <form class="form-inline" action="/urls/<%= shortURL %>" method="POST">
      <div class="form-group mb-2">
        <label for="newLongURL"></label>
        <input class="form-control" type="text" name="newLongURL" placeholder="<%= longURL %>" style="width: 300px; margin: 0em">
        <button type="submit" class="btn btn-primary">Update</button>
      </div>
    </form>
  <% } else { %>
    <h6>Please register or login to view your URLs. </h6>
  <% } %>
</body>
</html>